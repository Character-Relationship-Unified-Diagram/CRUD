import { NetworkGraph } from '../NetworkGraph/';

export const Dashboard = () => {
  // make fetch here
  // format data then dispatch to redux
  return (
    <>
      <NetworkGraph />
    </>
  );
};
